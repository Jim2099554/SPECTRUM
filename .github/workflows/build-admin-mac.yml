name: Build Admin - macOS

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-mac:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-python@v4
      with: { python-version: '3.11' }
    - uses: actions/setup-node@v3
      with: { node-version: '20' }
    
    - name: Cache
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-admin-${{ hashFiles('**/requirements.txt') }}
    
    - name: Install Deps
      run: |
        python -m pip install --upgrade pip
        python -m pip install wheel setuptools pyinstaller
        python -m pip install -r ./requirements.txt
        python -m pip install https://github.com/explosion/spacy-models/releases/download/es_core_news_sm-3.7.0/es_core_news_sm-3.7.0.tar.gz

    - name: Build Frontend
      run: |
        cd frontend
        npm install
        npm run build
        cd ..
        rm -rf backend/client
        cp -r frontend/dist backend/client

    - name: Create required directories
      run: |
        mkdir -p backend/audios
        mkdir -p backend/photos
        mkdir -p backend/transcripts

    - name: Build Executable
      run: python -m PyInstaller sentinela_admin.spec --clean --noconfirm

    - name: Package
      run: |
        PKG="SENTINELA-Admin-macOS-v1.0"
        mkdir -p "release/$PKG"
        cp -r dist/SENTINELA_Admin/* "release/$PKG/"
        cd release
        zip -r "$PKG.zip" "$PKG"

    - name: Upload
      uses: actions/upload-artifact@v4
      with:
        name: SENTINELA-Admin-macOS
        path: release/*.zip
